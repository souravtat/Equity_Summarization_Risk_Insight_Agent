# Core API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# HTTP client (used by evaluation scripts)
requests>=2.31.0

# Vector similarity search (FAISS fallback-safe; install CPU version)
faiss-cpu>=1.8.0

# Numerical computing (required by FaissRetriever)
numpy>=1.26.0

# LLM integration (optional — required only when GROQ_API_KEY is set)
groq>=0.9.0

# Development / evaluation extras
pylint>=3.1.0
pytest>=8.1.0
httpx>=0.27.0
